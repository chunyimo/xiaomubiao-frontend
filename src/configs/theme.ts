import { createMuiTheme } from '@material-ui/core/styles';
const theme = {
  ...createMuiTheme(),
  default: {
    firstColor: '#050b13',
    secondColor: '#fe5418',
    thirdColor: '#ffffff',
  }
};


export default theme;